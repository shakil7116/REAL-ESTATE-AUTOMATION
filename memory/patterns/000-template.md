# Pattern-000: Template
# ─────────────────────
# Copy this file to `patterns/slug.md` and fill in.
# Patterns are reusable solutions to recurring problems. Code-level, not
# architecture-level (architecture goes in decisions/).

## Name
Short, verb-noun. e.g. "Form Modal", "Optimistic Update", "Error Toast"

## Problem
What problem does this pattern solve? (1–2 sentences)

## When to Use
- Use when …
- Use when …
- Don't use when …

## Implementation
Step-by-step. Include code snippets.

```tsx
// Example
```

## Anti-Patterns
- ❌ Don't do X because …
- ❌ Don't do Y because …

## Examples in the Codebase
- `path/to/example.tsx:42` — uses this pattern
- `path/to/another.tsx:17` — uses this pattern
